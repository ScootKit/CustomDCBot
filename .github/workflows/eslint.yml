name: Check style

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v
      - name: Install dependencies
        run: npm install
      - name: Run ESLint
        run: eslint ./